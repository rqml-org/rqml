---
title: RQML
description: Requirements you can compile. Make specs the source of truth—not code.
slug: /
---

import Heading from '@theme/Heading';
import CodeBlock from '@theme/CodeBlock';
import RqmlTagExplorer from '@site/src/components/RqmlTagExplorer';
import styles from './index.module.css';

<div className={styles.page}>

  {/* HERO: first */}
  <header className={`hero ${styles.hero}`}>
    <div className="container">

      <img id="heroLogo" width="25%" src="/img/RQML_logo_transparent.png" alt="RQML logo" className={styles.heroLogo} />
      <Heading as="h1" className={styles.centerHeader}>
        Spec-first development for the LLM era.
      </Heading>

      <p size="lg" className={styles.heroSubtitleCenter}>
        <u>R</u>e<u>q</u>uirements <u>M</u>arkup <u>L</u>anguage is an LLM-first requirements 
        format that gives coding agents <b>durable project context</b> — so your intent, constraints, 
        and acceptance criteria stay clear from planning to implementation.
      </p>

      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/quick-start">Get started</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">View on GitHub</a>
        <a className="button button--secondary button--lg" href="https://marketplace.visualstudio.com/items?itemName=rqml.rqml-vscode" target="_blank" rel="noopener noreferrer">Try it in VS Code</a>
      </div>

      <p className={styles.heroSubtitleCenter}>
        Scaffold a spec and an <code>AGENTS.md</code> in one command — works with any coding agent:
      </p>
      <CodeBlock language="bash">{`npx @rqml/cli init`}</CodeBlock>

    </div>
  </header>

  {/* WHY XML — disarm the most common objection early */}
  <section className={`container ${styles.section}`}>
    <div className="container">
      <p size="lg" className={styles.heroSubtitleCenter}>
        <b>Why XML — and not JSON or Markdown?</b> Because the model reads it best.
        Anthropic, Google, and AWS all recommend XML tags to structure LLM prompts;
        RQML turns your spec into a self-validating version of exactly that — strict
        where it matters, readable everywhere else.
      </p>
      <div className={styles.heroCtas}>
        <a className="button button--secondary button--lg" href="/why-xml">Why XML</a>
        <a className="button button--secondary button--lg" href="/why-rqml">Why RQML</a>
      </div>
    </div>
  </section>

  {/* HERO: Workflow */}
  <section id="workflow" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>
        RQML is the missing piece in your LLM workflow.
      </Heading>
      <div className={styles.heroImageWrap}>
        <img
          className={styles.heroImageWorkflow}
          src="/img/hero_workflow.svg"
          alt="RQML workflow illustrating goals, requirements, and verification"
        />
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--secondary button--lg" href="/why-rqml">Read the rationale</a>
      </div>
    </div>
  </section>

  {/* HERO: examples */}
  <section id="examples" className={`${styles.hero}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>
        Your LLM writes it. Your code gets better.
      </Heading>
      <p size="lg" className={styles.heroSubtitleCenter}>
        An RQML document is organized into 11 mostly optional sections, 
        each capturing a different aspect of your requirements.
      </p>
      <div className={styles.heroCtas}>
        <a className="button button--secondary button--lg" href="/why-xml">Why XML</a>
      </div>
      <p/>
      <RqmlTagExplorer />
    </div>
    <div className={styles.heroCtas}>
      <a className="button button--primary button--lg" href="/docs/introduction">Read the docs</a>
      <a className="button button--secondary button--lg" href="https://marketplace.visualstudio.com/items?itemName=rqml.rqml-vscode" target="_blank" rel="noopener noreferrer">Get the VS Code extension</a>
    </div>
  </section>  

  {/* TOOLING */}
  <section id="tooling" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>
        Not just a format — a deterministic toolchain.
      </Heading>
      <p size="lg" className={styles.heroSubtitleCenter}>
        One engine powers a CLI, an MCP server for your agent, and the VS Code extension.
        It's open source (Apache&#8209;2.0), published on npm, runs fully offline, and keeps
        <b> no language model in the verdict path</b> — so <code>rqml check</code> is reproducible
        enough to gate CI on.
      </p>

      <div className={styles.grid3}>
        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>rqml — the CLI</Heading>
            <p className={styles.cardText}>
              <code>init</code>, <code>validate</code>, <code>status</code>, and a deterministic
              <code> check</code> gate for CI and editor/commit hooks.
            </p>
            <p className={styles.cardText}><code>npm i -g @rqml/cli</code></p>
          </div>
        </div>

        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>@rqml/core — the engine</Heading>
            <p className={styles.cardText}>
              Parse, validate (XSD + referential integrity), lint, trace, coverage, and drift —
              embed it in your own tools.
            </p>
            <p className={styles.cardText}><code>npm i @rqml/core</code></p>
          </div>
        </div>

        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>@rqml/mcp — for agents</Heading>
            <p className={styles.cardText}>
              A Model Context Protocol server that gives any coding agent RQML tools to
              validate and check specs.
            </p>
            <p className={styles.cardText}><code>npx @rqml/mcp</code></p>
          </div>
        </div>
      </div>

      <p size="lg" className={styles.heroSubtitleCenter}>
        Link a requirement to the code that implements it, and <code>rqml check</code> fails —
        in CI or a commit hook — the moment that code drifts:
      </p>
      <CodeBlock language="bash">{`$ rqml check
✓ check pass (standard) — requirements.rqml

# after the code an "implements" edge points to is deleted:
$ rqml check
  error (drift) [missing-implementation]: implements edge "E-IMPL-AUTH"
    points at "file:src/auth.ts", which does not exist.
✗ check fail (standard) — requirements.rqml      (exit code 2)`}</CodeBlock>

      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/tooling">Explore the tooling</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">Star on GitHub</a>
      </div>
    </div>
  </section>

  {/* HERO: Features */}
  {/*
  <section id="features" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>
        RQML is requirements engineering done properly - benefiting LLMs and humans alike.
      </Heading>
      <div className={styles.heroImageWrap}>
        <img
          className={styles.heroImageFeatures}
          src="/img/hero_features.svg"
          alt="RQML features illustration"
        />
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/user-guide">Open the user guide</a>
        <a className="button button--secondary button--lg" href="/docs/reference">Browse the reference</a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: WHY NOW */}
  {/* 
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">LLMs amplify weak requirements.</Heading>
        <p className={styles.lead}>
          Teams already cut corners on requirements because writing “proper specs” feels slow and unrewarding.
          LLM-assisted coding makes that problem sharper.
        </p>

        <ul className={styles.bullets}>
          <li>AI is fast—but it <strong>guesses context</strong> unless you provide it.</li>
          <li>You get <strong>prompt soup</strong>, inconsistent decisions, and fragile architecture.</li>
          <li>Docs rot because they’re <strong>manual</strong>, boring, and hard to keep aligned.</li>
        </ul>

        <p className={styles.punch}>
          If you’re using LLMs to write code, you need a better way to communicate intent than scattered chats and
          half-remembered assumptions.
        </p>

        <a className="button button--outline button--primary" href="/docs/why-rqml">
          Read the rationale →
        </a>
      </div>

      <div className={styles.cardStack}>
        <div className={`card ${styles.card}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>Before</Heading>
            <p className={styles.cardText}>
              A thousand tiny assumptions in chat logs, tickets, and tribal knowledge.
            </p>
            <div className={styles.tagRow}>
              <span className={styles.tag}>Ambiguous</span>
              <span className={styles.tag}>Hard to reuse</span>
              <span className={styles.tag}>Easy to forget</span>
            </div>
          </div>
        </div>

        <div className={`card ${styles.card}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>After</Heading>
            <p className={styles.cardText}>
              One versioned requirements spec that both humans and LLM tools can rely on.
            </p>
            <div className={styles.tagRow}>
              <span className={styles.tag}>Explicit</span>
              <span className={styles.tag}>Toolable</span>
              <span className={styles.tag}>Diff-friendly</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  */}

  {/* HERO: WHAT IT CHANGES */}
  {/*
  <section className={`${styles.sectionAlt}`}>
    <div className="container">
      <div className={styles.centerHeader}>
        <Heading as="h2">A shared understanding—between humans and machines.</Heading>
        <p className={styles.lead}>
          RQML gives your project a single place to store the “why” and “what” of the system in a way that scales with
          your codebase and your team.
        </p>
      </div>

      <div className={styles.grid3}>
        {[
          {
            title: 'Clear structure',
            text: 'Goals, scenarios, requirements, interfaces, verification, traceability—organized so nothing important gets lost.',
          },
          {
            title: 'Low ceremony',
            text: 'Enough Requirements Engineering discipline to be useful—without turning spec writing into a second job.',
          },
          {
            title: 'LLM-ready',
            text: 'Predictable, structured context that improves codegen, refactors, tests, and architecture decisions.',
          },
          {
            title: 'Diff-friendly',
            text: 'Review requirements like code. PRs show exactly what changed and why.',
          },
          {
            title: 'Traceable',
            text: 'Connect goals → requirements → verification so you can prove what you built matches intent.',
          },
          {
            title: 'Toolable',
            text: 'Schema validation, editor support, CI checks, and automated consistency improvements.',
          },
        ].map((f) => (
          <div key={f.title} className={`card ${styles.featureCard}`}>
            <div className="card__body">
              <Heading as="h3" className={styles.cardTitle}>{f.title}</Heading>
              <p className={styles.cardText}>{f.text}</p>
            </div>
          </div>
        ))}
      </div>

      <div className={styles.ctaRow}>
        <a className="button button--primary" href="/docs/user-guide">
          Open the user guide
        </a>
        <a className="button button--secondary" href="/docs/reference">
          Browse the reference
        </a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: HOW */}
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">Start right now</Heading>
        <ol className={styles.steps}>
          <li>
            <strong>Scaffold</strong> — <code>npx @rqml/cli init</code> drops a <code>requirements.rqml</code> and an <a href="/AGENTS.md" download><code>AGENTS.md</code></a> in your project root
          </li>
          <li>
            <strong>Check</strong> — <code>npx @rqml/cli check</code> validates the schema, coverage, and implementation drift (deterministic, offline)
          </li>
          <li>
            Develop with your <strong>coding agent of choice</strong> — it uses RQML as the source of truth for system intent, and you gate CI with <code>rqml check</code>
          </li>
        </ol>

        <p className={styles.punch}>
          <b>Tip:</b> Don't write RQML from scratch. Grab the official VS Code extension on 
          <a href="http://rqml.dev" target="_blank" rel="noopener noreferrer">RQML.dev</a> 
          for authoring support, real-time schema validation, spec browsing and export. There's even a built-in agent.
        </p>

        <div className={styles.heroCtas}>
          <a className="button button--primary button--lg" href="/docs/quick-start">Quick start</a>
        </div>
      </div>

      <div className={`card ${styles.snippetCard}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>Hello world RQML example</Heading>
          <CodeBlock language="xml">
{`<rqml xmlns="https://rqml.org/schema/2.1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="https://rqml.org/schema/2.1.0
        https://rqml.org/schema/rqml-2.1.0.xsd"
      version="2.1.0" docId="DOC-HELLO-001" status="draft">
  <meta>
    <title>Hello World CLI</title>
    <system>hello</system>
  </meta>
  <requirements>
    <req id="REQ-HELLO-001" type="FR" title="Print greeting"
         status="draft" priority="must">
      <statement>
        The program MUST print "Hello, world!" to standard output
        and exit with status code 0.
      </statement>
    </req>
  </requirements>
</rqml>`}
          </CodeBlock>
        </div>
      </div>
    </div>
  </section>

  {/* SUPPORTING: USE CASES */}
  {/*
  <section className={`${styles.sectionAlt}`}>
    <div className="container">
      <div className={styles.centerHeader}>
        <Heading as="h2">Works especially well for</Heading>
        <p className={styles.lead}>Where intent gets lost, RQML keeps it explicit and reusable.</p>
      </div>

      <div className={styles.grid2}>
        {[
          {
            title: 'Greenfield projects',
            text: 'Reduce back-and-forth prompting, align faster, and establish a stable architecture narrative.',
          },
          {
            title: 'Refactors',
            text: 'Preserve intent while changing internals—so the “why” survives redesigns.',
          },
          {
            title: 'Teams & handoffs',
            text: 'Make decisions portable. New teammates and tools can catch up without archaeology.',
          },
          {
            title: 'Regulated / critical systems',
            text: 'Traceability + verification paths without heavyweight spec tooling overhead.',
          },
        ].map((u) => (
          <div key={u.title} className={`card ${styles.featureCard}`}>
            <div className="card__body">
              <Heading as="h3" className={styles.cardTitle}>{u.title}</Heading>
              <p className={styles.cardText}>{u.text}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  */}

  {/* SUPPORTING: MARKDOWN COMPARISON */}
  {/*
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">Why not just Markdown?</Heading>
        <p className={styles.lead}>
          Markdown is great for humans—but it’s flexible in ways that make it unreliable as machine-consumable system intent.
        </p>

        <ul className={styles.bullets}>
          <li>Markdown is <strong>ambiguous</strong> unless you enforce strict conventions.</li>
          <li>RQML is structured enough to be <strong>consumed consistently</strong> by tools and LLMs.</li>
          <li>You still get human context—just with a <strong>predictable shape</strong>.</li>
        </ul>

        <a className="button button--outline button--primary" href="/docs/reference">
          See the tag/attribute index →
        </a>
      </div>

      <div className={`card ${styles.card}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>The goal</Heading>
          <p className={styles.cardText}>
            Make the requirements spec the durable artifact. Let code be generated, regenerated, and refactored around it.
          </p>
          <div className={styles.quote}>
            “Stop re-explaining the same context to every tool, every time.”
          </div>
        </div>
      </div>
    </div>
  </section>
  */}

  {/* FINAL CTA */}
  <section className={styles.final}>
    <div className="container">
      <Heading as="h2" className={styles.finalTitle}>Try RQML on your next project.</Heading>
      <p size="lg" className={styles.heroSubtitleCenter}>
        Put your intent in the repo. Make LLM output calmer, cleaner, and easier to trust. And finally start doing requirements right.
      </p>

      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/quick-start">Get started</a>
        <a className="button button--secondary button--lg" href="/docs/tooling">Explore the tooling</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">Star on GitHub</a>
      </div>
    </div>
  </section>

</div>
