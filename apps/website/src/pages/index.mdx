---
title: RQML
description: Coding agents drift from the spec. RQML fails the build — an open requirements format plus a deterministic toolchain that traces the spec to code and tests. No LLM in the verdict path.
slug: /
---

import Heading from '@theme/Heading';
import CodeBlock from '@theme/CodeBlock';
import styles from './index.module.css';

{/* import RqmlTagExplorer from '@site/src/components/RqmlTagExplorer';
    — commented out with the tag-explorer section below; restore both together. */}

<div className={styles.page}>

  {/* HERO: first */}
  <header className={`hero ${styles.hero}`}>
    <div className="container">

      <img id="heroLogo" src="/img/RQML_logo_transparent.png" alt="RQML logo" className={styles.heroLogo} />
      <div className={styles.heroKicker}>Requirements Markup Language — an open format for requirements, plus a deterministic toolchain</div>
      <Heading as="h1" className={styles.centerHeader}>Agents drift from the spec. RQML fails the build.</Heading>

      <div className={styles.heroSubtitleCenter}>
        Your agent drafts the spec. <code>rqml link</code> pins each requirement to the code
        and tests that realize it. When they drift apart, <code>rqml check</code> fails the
        build — and, with the plugins, the agent's turn. <b>No language model in the
        verdict path.</b>
      </div>
      <div className={styles.heroSubtitleCenter}>
        <b>Built for
        codebases where an agent writes much of the code</b> — and someone still has to know
        what the system is supposed to do.
      </div>

      {/* Pre-restructure hero (2026-07), superseded by the copy above. Kept for reference:
      <Heading as="h1" className={styles.centerHeader}>Make the spec the artifact. Then gate CI on it.</Heading>

      <div className={styles.heroSubtitleCenter}>
        <b>Requirements Markup Language</b> is an XML format for software
        requirements (which your agent will <b>write for you</b>) with a deterministic toolchain: validate the spec, trace it to the code
        and tests that realize it, and fail the build when they drift apart.
      </div>
      */}

      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/quick-start">Get started</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">View on GitHub</a>
      </div>

      <div className={`${styles.tagRow} ${styles.heroBadges}`}>
        <span className={styles.tag}>Apache-2.0</span>
        <span className={styles.tag}>open schema</span>
        <span className={styles.tag}>fully offline</span>
        <span className={styles.tag}>no server, no API keys</span>
        <span className={styles.tag}>works with any coding agent</span>
      </div>

      <div className={`${styles.scaffoldNote}`}>
        <center>One command scaffolds a spec and an <code>AGENTS.md</code>:</center>
      </div>
      <div className={styles.heroCode}>
        <CodeBlock language="bash">{`npx @rqml/cli init`}</CodeBlock>
      </div>

      {/* DEMO GIF — run `vhs demo.tape` in static/img/ to generate demo.gif, then uncomment:
      <div className={styles.heroImageWrap}>
        <img src="/img/demo.gif" alt="rqml init then rqml check passing in a terminal" width="900" />
      </div>
      */}

    </div>
  </header>

  {/* THE PITCH, IN ONE TERMINAL SESSION */}
  <section className={`${styles.section} ${styles.breakout}`}>
    <div className="container">
      {/* was: "The whole idea, in one terminal session" */}
      <Heading as="h2" className={styles.centerHeader}>Six weeks later, the agent quietly rewrote auth. <code>rqml check</code> noticed.</Heading>

      <div className={styles.heroSubtitleCenter}>
        Spec-driven tools like Spec Kit and Kiro get you a spec. RQML makes it binding:
      </div>

      {/* TODO: side-by-side validation demo — spec on the left, live rqml check verdicts on the right */}

      <div className={styles.terminal}>
        <div className={styles.terminalBar}>
          <span className={`${styles.termDot} ${styles.termDotRed}`} />
          <span className={`${styles.termDot} ${styles.termDotYellow}`} />
          <span className={`${styles.termDot} ${styles.termDotGreen}`} />
          <span className={styles.terminalTitle}>fish — ~/myproject</span>
        </div>
        <pre className={styles.terminalBody}>
          <span className={styles.termLine}><span className={styles.tPath}>~/myproject</span> <span className={styles.tPrompt}>❯</span> <span className={styles.tCmd}>rqml show REQ-AUTH-001</span></span>
          <span className={styles.termLine}><span className={styles.tHl}>## REQ-AUTH-001 — Verify session tokens</span></span>
          <span className={styles.termLine}><span className={styles.tDim}>kind: req (FR) · status: approved · priority: must</span></span>
          <span className={styles.termLine}>{' '}</span>
          <span className={styles.termLine}><span className={styles.tOut}>The API MUST reject any request whose session token fails signature</span></span>
          <span className={styles.termLine}><span className={styles.tOut}>verification, with status 401.</span></span>
          <span className={styles.termLine}>{' '}</span>
          <span className={styles.termLine}><span className={styles.tDim}># the agent implements it, then records the link</span></span>
          <span className={styles.termLine}><span className={styles.tPath}>~/myproject</span> <span className={styles.tPrompt}>❯</span> <span className={styles.tCmd}>rqml link REQ-AUTH-001 src/auth.ts#verifyToken</span></span>
          <span className={styles.termLine}><span className={styles.tOk}>✓ REQ-AUTH-001 ← src/auth.ts#verifyToken</span><span className={styles.tOut}> (E-IMPL-AUTH-001, implements, baseline recorded)</span></span>
          <span className={styles.termLine}>{' '}</span>
          <span className={styles.termLine}><span className={styles.tPath}>~/myproject</span> <span className={styles.tPrompt}>❯</span> <span className={styles.tCmd}>rqml check</span></span>
          <span className={styles.termLine}><span className={styles.tOk}>✓ check pass (standard)</span><span className={styles.tOut}> — requirements.rqml</span></span>
          <span className={styles.termLine}>{' '}</span>
          <span className={styles.termLine}><span className={styles.tDim}># six weeks later, an agent refactors auth without touching the spec…</span></span>
          <span className={styles.termLine}><span className={styles.tPath}>~/myproject</span> <span className={styles.tPrompt}>❯</span> <span className={styles.tCmd}>rqml check</span></span>
          <span className={styles.termLine}>{'  '}<span className={styles.tErr}>error (drift) [changed-implementation]:</span><span className={styles.tOut}> implements edge "E-IMPL-AUTH-001"</span></span>
          <span className={styles.termLine}>{'    '}<span className={styles.tOut}>points at "src/auth.ts#verifyToken", which has changed since approval.</span></span>
          <span className={styles.termLine}><span className={styles.tErr}>✗ check fail (standard) — requirements.rqml</span>{'      '}<span className={styles.tDim}># exit 2</span></span>
        </pre>
      </div>

      <div className={styles.heroSubtitleCenter}>
        <b>No AI checks the AI — a checksum does.</b> Drift means pinned code changed and
        nobody re-reviewed the link; the gate forces a look. <code>rqml check</code> is a
        pure function of your repository — same input, same verdict, on your laptop and in
        CI. The model proposes; the toolchain disposes.
      </div>
    </div>
  </section>

  {/* START IN THREE STEPS — sits directly after the demo so install follows understanding */}
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">Start in three steps</Heading>
        <div className={styles.lead}>
          A useful spec fits on one screen; a serious one scales to thousands of
          requirements with goals, scenarios, state machines, and a typed traceability graph.
        </div>
        <ol className={styles.steps}>
          <li>
            <strong>Scaffold</strong> — <code>npx @rqml/cli init</code> drops a <code>requirements.rqml</code> and an <a href="/AGENTS.md" download><code>AGENTS.md</code></a> in your project root
          </li>
          <li>
            <strong>Check</strong> — <code>npx @rqml/cli check</code> validates the schema, checks that every requirement is linked to code and tests, and catches implementation drift
          </li>
          <li>
            Develop with your <strong>coding agent of choice</strong> — it works from the spec, records trace links with <code>rqml link</code>, and you gate CI with <code>rqml check</code>
          </li>
        </ol>

        <div className={styles.punch}>
          <b>Tip:</b> <code>rqml skeleton</code> keeps the structure valid while your agent
          drafts, and the <a href="#plugins">Claude Code and Codex plugins</a> run the whole
          loop for you.
        </div>

        <div className={styles.heroCtas}>
          <a className="button button--primary button--lg" href="/docs/quick-start">Quick start</a>
        </div>
      </div>

      <div className={`card ${styles.snippetCard}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>A complete, valid spec</Heading>
          <CodeBlock language="xml">
{`<rqml xmlns="https://rqml.org/schema/2.2.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="https://rqml.org/schema/2.2.0
        https://rqml.org/schema/rqml-2.2.0.xsd"
      version="2.2.0" docId="DOC-HELLO-001" status="draft">
  <meta>
    <title>Hello World CLI</title>
    <system>hello</system>
  </meta>
  <requirements>
    <req id="REQ-HELLO-001" type="FR" title="Print greeting"
         status="draft" priority="must">
      <statement>
        The program MUST print "Hello, world!" to standard output
        and exit with status code 0.
      </statement>
    </req>
  </requirements>
</rqml>`}
          </CodeBlock>
          <div className={styles.cardText}>
            The header boilerplate is scaffolded by <code>rqml init</code> and maintained by
            your agent — what you review is the <code>statement</code>.
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* AGENT PLUGINS — split back out of "One engine, every surface" in the reorder */}
  <section id="plugins" className={`${styles.section} ${styles.breakout} ${styles.bandPlugins}`}>
    <div className="container">
      <Heading as="h2" className={styles.centerHeader}>Drop it into your coding agent</Heading>
      <div className={styles.heroSubtitleCenter}>
        The <b>Claude Code</b> and <b>Codex</b> plugins run the whole loop for you:
        every session opens anchored on your spec, every edit is validated in the same
        turn, and the turn is gated on <code>rqml check</code> — the same verdict your
        CI runs.
      </div>

      {/* Pre-restructure intro (2026-07), superseded by the copy above. Kept for reference:
      <Heading as="h1" className={styles.centerHeader}>Drop it into your coding agent</Heading>
      <div className={styles.heroSubtitleCenter}>
        The <b>Claude Code</b> and <b>Codex</b> plugins make the CLI loop above
        automatic: every session opens anchored on your spec, every <code>.rqml</code> edit is
        validated in the same turn, and the turn is gated on <code>rqml check</code> — the same
        deterministic verdict your CI runs. The plugins ship no engine of their own; they shell
        out to <code>@rqml/cli</code>, so the agent is held to the same gate as your build.
      </div>
      */}

      <div className={styles.grid2}>
        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <div className={styles.tagRow}>
              <span className={styles.tag}>Early access · v0.1.0</span>
            </div>
            <Heading as="h3" className={styles.cardTitle}>Claude Code plugin</Heading>
            <div className={styles.cardText}>
              Anchor, validate, and gate Claude in every session. Six <code>/rqml:*</code> commands
              for the Spec → Design → Plan → Code → Verify workflow, plus the bundled MCP tools and an
              RQML authoring skill.
            </div>
            <CodeBlock language="bash">{`npm install -g @rqml/cli
# then, inside Claude Code:
/plugin marketplace add rqml-org/rqml-claude
/plugin install rqml@rqml`}</CodeBlock>
            <div className={styles.heroCtas}>
              <a className="button button--primary button--lg" href="https://github.com/rqml-org/rqml-claude" target="_blank" rel="noopener noreferrer">Get the Claude Code plugin</a>
            </div>
          </div>
        </div>

        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <div className={styles.tagRow}>
              <span className={styles.tag}>Early access · v0.1.0</span>
            </div>
            <Heading as="h3" className={styles.cardTitle}>Codex plugin</Heading>
            <div className={styles.cardText}>
              The same loop for OpenAI Codex: session anchoring, in-turn spec validation, and a
              stop-time <code>rqml check</code> gate.
            </div>
            <CodeBlock language="bash">{`npm install -g @rqml/cli
# then, inside Codex:
codex plugin marketplace add rqml-org/rqml-codex
# enable the RQML entry, then trust its hooks`}</CodeBlock>
            <div className={styles.heroCtas}>
              <a className="button button--primary button--lg" href="https://github.com/rqml-org/rqml-codex" target="_blank" rel="noopener noreferrer">Get the Codex plugin</a>
            </div>
          </div>
        </div>
      </div>

      <div className={styles.heroSubtitleCenter}>
        On a different host? The <a href="https://github.com/rqml-org/rqml-skill" target="_blank" rel="noopener noreferrer">RQML Agent Skill</a> brings
        the same workflow to any skill-compatible agent, and the <a href="https://marketplace.visualstudio.com/items?itemName=rqml.rqml-vscode" target="_blank" rel="noopener noreferrer">VS Code extension</a> adds
        in-editor authoring and export. Plugins enforce; these assist.
      </div>
    </div>
  </section>

  {/* ONE ENGINE, EVERY SURFACE — the toolchain behind the plugins */}
  <section className={`${styles.section} ${styles.breakout}`}>
    <div className="container">
      <Heading as="h2" className={styles.centerHeader}>One engine, every surface</Heading>
      <div className={styles.heroSubtitleCenter}>
        One open-source engine — Apache&#8209;2.0, published on npm, no telemetry — powers
        the CLI, an MCP server, and the agent plugins.
      </div>

      <div className={styles.heroSubtitleCenter}>
        The loop an agent runs — read one requirement, check the blast radius, implement,
        record the link, pass the gate:
      </div>
      <div className={styles.heroCode}>
        <CodeBlock language="bash">{`rqml show REQ-PAY-001     # one requirement: statement, acceptance criteria, trace neighborhood
rqml impact REQ-PAY-001   # what is affected, transitively, if this changes?
rqml link REQ-PAY-001 src/payments/capture.ts   # implements edge + content-hash baseline
rqml check                # validation + coverage + drift; exit 0 or it isn't done`}</CodeBlock>
      </div>

      <div className={styles.heroSubtitleCenter}>
        Under the hood it's three npm packages — <a href="/docs/tooling">explore the tooling →</a>
      </div>
      <ul className={styles.packagesList}>
        <li><code>@rqml/cli</code> — <code>init</code>, <code>validate</code>, <code>status</code>, and the <code>check</code> gate, plus the agent loop: <code>show</code>, <code>impact</code>, <code>link</code>, <code>skeleton</code>.</li>
        <li><code>@rqml/core</code> — the engine: parse, validate, lint, trace, impact, coverage, drift. Embed it in your own tools.</li>
        <li><code>@rqml/mcp</code> — thirteen Model Context Protocol tools backed by the same engine, for any MCP-capable agent.</li>
      </ul>
    </div>
  </section>

  {/* TOOLING — superseded by the merged "One engine, every surface" section above
      (2026-07 restructure). Kept for reference:
  <section id="tooling" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>Not just a format — a deterministic toolchain.</Heading>
      <div className={styles.heroSubtitleCenter}>
        One engine powers a CLI, an MCP server for your agent, and the VS Code extension.
        Open source (Apache&#8209;2.0), published on npm, fully offline, no telemetry, and
        <b> no language model in the verdict path</b>.
      </div>

      <div className={styles.grid3}>
        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>rqml — the CLI</Heading>
            <div className={styles.cardText}>
              <code>init</code>, <code>validate</code>, <code>status</code>, and the
              <code> check</code> gate — plus the agent loop: <code>show</code>,
              <code> impact</code>, <code>link</code>, <code>skeleton</code>.
            </div>
            <p className={styles.cardText}><code>npm i -g @rqml/cli</code></p>
          </div>
        </div>

        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>@rqml/core — the engine</Heading>
            <div className={styles.cardText}>
              Parse, validate (XSD + referential integrity), lint, trace, impact, coverage,
              drift, and comment-preserving spec edits — embed it in your own tools.
            </div>
            <p className={styles.cardText}><code>npm i @rqml/core</code></p>
          </div>
        </div>

        <div className={`card ${styles.featureCard}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>@rqml/mcp — for agents</Heading>
            <div className={styles.cardText}>
              Thirteen Model Context Protocol tools backed by the same engine. Reads specs by
              file path (no pasting 30k tokens into a tool call); writes only on explicit
              intent.
            </div>
            <p className={styles.cardText}><code>npx @rqml/mcp</code></p>
          </div>
        </div>
      </div>
      <p/>
      <div className={styles.heroSubtitleCenter}>
        The loop an agent actually runs — read one requirement, check the blast radius,
        implement, record the link, pass the gate:
      </div>
      <CodeBlock language="bash">{`rqml show REQ-PAY-001     # one requirement: statement, acceptance criteria, trace neighborhood
rqml impact REQ-PAY-001   # what is affected, transitively, if this changes?
rqml link REQ-PAY-001 src/payments/capture.ts   # implements edge + content-hash baseline
rqml check                # validation + coverage + drift; exit 0 or it isn't done`}</CodeBlock>

      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/tooling">Explore the tooling</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">Star on GitHub</a>
      </div>
    </div>
  </section>
  */}

  {/* TRUST BAND — dogfood + what-it-is-not + XML one-liner (2026-07 restructure) */}
  <section className={`container ${styles.section} ${styles.bandDogfood}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">It eats its own dog food.</Heading>
        <div className={styles.lead}>
          The RQML repository is specified in RQML — <code>requirements.rqml</code> in the
          repo root. Every shipped feature was specified before it was built and is traced
          to the code that implements it. The repo gates its own CI
          with <code>rqml check</code>. <a href="https://github.com/rqml-org/rqml/blob/main/requirements.rqml" target="_blank" rel="noopener noreferrer">Read the self-spec →</a>
        </div>
        <div className={styles.lead}>
          Yes, it's XML — deliberately. Requirements are documents, not data records, and
          XML is boring in useful ways: schemas, mixed content, deterministic
          validation. <a href="/why-xml">Why XML →</a>
        </div>
        <div className={styles.lead}>
          Need requirement-to-test traceability you can show an auditor? The trace graph is
          plain XML in your repo — the VS Code extension renders it as a traceability map
          and exports documents.
        </div>
        {/* Origin story, kept for reference (also in the FAQ):
        <div className={styles.lead}>
          The name is older than you might guess: RQML began as an XML DTD in a 2000 MSc
          thesis at the University of York. 2.x is a ground-up redesign of that idea for
          coding agents.
        </div>
        <div className={styles.heroCtas}>
          <a className="button button--primary button--lg" href="https://github.com/rqml-org/rqml/blob/main/requirements.rqml" target="_blank" rel="noopener noreferrer">Read the self-spec →</a>
          <a className="button button--secondary button--lg" href="/docs/faq/#is-rqml-really-25-years-old">The origin story</a>
        </div>
        */}
      </div>

      <div className={`card ${styles.card}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>What RQML is not</Heading>
          <ul className={styles.bullets}>
            <li><strong>Not a code generator.</strong> It never writes your code — your agent does that. RQML is what keeps the agent honest.</li>
            <li><strong>Not AI-powered.</strong> No model runs in the verdict path. Verdicts are reproducible functions of your repo.</li>
            {/* Replaced 2026-07 (name means little to the agent-native audience):
            <li><strong>Not DOORS.</strong> A text file in your repo and a small npm package — not an enterprise requirements suite.</li>
            */}
            <li><strong>Not a platform.</strong> Plain files in your repo and a small npm package — no server, no dashboard, no account.</li>
            <li><strong>Not ceremony.</strong> <code>meta</code> plus one requirement is a valid spec. Everything else is optional and added when it earns its keep.</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  {/* FINAL CTA — full-width band immediately before the footer */}
  <section className={styles.final}>
    <div className="container">
      <Heading as="h2" className={styles.finalTitle}>Keep your agent and your spec in sync</Heading>
      <div className={styles.heroCode}>
        <CodeBlock language="bash">{`npx @rqml/cli init`}</CodeBlock>
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/quick-start">Start in five minutes</a>
        <a className="button button--secondary button--lg" href="https://github.com/rqml-org/rqml" target="_blank" rel="noopener noreferrer">View on GitHub</a>
      </div>
    </div>
  </section>

  {/* WHY XML — collapsed to a one-liner in the trust band above (2026-07 restructure).
      Kept for reference:
  <section className={styles.xmlCallout}>
    <div className="container">
    <Heading as="h1" className={styles.centerHeader}>Yes, XML</Heading>
      <div className={styles.heroSubtitleCenter}>
        RQML uses XML because requirements are documents, not just data records.
      </div>
      <div className={styles.heroSubtitleCenter}>
        They mix prose, structure, identifiers, references, comments, validation
        rules, and future extensions. YAML, TOML, and JSON are excellent for
        configuration; RQML needs structured text.
      </div>
      <div className={styles.heroSubtitleCenter}>
        XML is not fashionable, but it is boring in useful ways: mixed content,
        schemas, namespaces, and deterministic validation.
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--secondary button--lg" href="/why-xml">Why XML</a>
        <a className="button button--secondary button--lg" href="/why-rqml">Why RQML</a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: examples — tag explorer cut in the 2026-07 restructure (its default snippet
      read as ceremony; the "fits on one screen" claim moved to "Start in three steps").
      Kept for reference:
  <section id="examples" className={`${styles.hero}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>Eleven sections. Two are mandatory.</Heading>
      <div className={styles.heroSubtitleCenter}>
        A useful spec fits in 15 lines; a serious one scales to thousands of requirements
        with goals, scenarios, state machines, and a typed traceability graph. Explore the
        vocabulary:
      </div>
      <RqmlTagExplorer />
    </div>
    <div className={styles.heroCtas}>
      <a className="button button--primary button--lg" href="/docs/introduction">Read the docs</a>
      <a className="button button--secondary button--lg" href="/docs/quick-start">Get started</a>
    </div>
  </section>
  */}

  {/* HERO: Workflow — superseded by the terminal demo above; the SVG says less than the shell does
  <section id="workflow" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>RQML is the missing piece in your LLM workflow.</Heading>
      <div className={styles.heroImageWrap}>
        <img
          className={styles.heroImageWorkflow}
          src="/img/hero_workflow.svg"
          alt="RQML workflow illustrating goals, requirements, and verification"
        />
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--secondary button--lg" href="/why-rqml">Read the rationale</a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: Features */}
  {/*
  <section id="features" className={`container ${styles.section}`}>
    <div className="container">
      <Heading as="h1" className={styles.centerHeader}>RQML is requirements engineering done properly - benefiting LLMs and humans alike.</Heading>
      <div className={styles.heroImageWrap}>
        <img
          className={styles.heroImageFeatures}
          src="/img/hero_features.svg"
          alt="RQML features illustration"
        />
      </div>
      <div className={styles.heroCtas}>
        <a className="button button--primary button--lg" href="/docs/user-guide">Open the user guide</a>
        <a className="button button--secondary button--lg" href="/docs/reference">Browse the reference</a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: WHY NOW */}
  {/* 
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">LLMs amplify weak requirements.</Heading>
        <div className={styles.lead}>
          Teams already cut corners on requirements because writing “proper specs” feels slow and unrewarding.
          LLM-assisted coding makes that problem sharper.
        </div>

        <ul className={styles.bullets}>
          <li>AI is fast—but it <strong>guesses context</strong> unless you provide it.</li>
          <li>You get <strong>prompt soup</strong>, inconsistent decisions, and fragile architecture.</li>
          <li>Docs rot because they’re <strong>manual</strong>, boring, and hard to keep aligned.</li>
        </ul>

        <div className={styles.punch}>
          If you’re using LLMs to write code, you need a better way to communicate intent than scattered chats and
          half-remembered assumptions.
        </div>

        <a className="button button--outline button--primary" href="/docs/why-rqml">
          Read the rationale →
        </a>
      </div>

      <div className={styles.cardStack}>
        <div className={`card ${styles.card}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>Before</Heading>
            <div className={styles.cardText}>
              A thousand tiny assumptions in chat logs, tickets, and tribal knowledge.
            </div>
            <div className={styles.tagRow}>
              <span className={styles.tag}>Ambiguous</span>
              <span className={styles.tag}>Hard to reuse</span>
              <span className={styles.tag}>Easy to forget</span>
            </div>
          </div>
        </div>

        <div className={`card ${styles.card}`}>
          <div className="card__body">
            <Heading as="h3" className={styles.cardTitle}>After</Heading>
            <div className={styles.cardText}>
              One versioned requirements spec that both humans and LLM tools can rely on.
            </div>
            <div className={styles.tagRow}>
              <span className={styles.tag}>Explicit</span>
              <span className={styles.tag}>Toolable</span>
              <span className={styles.tag}>Diff-friendly</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  */}

  {/* HERO: WHAT IT CHANGES */}
  {/*
  <section className={`${styles.sectionAlt}`}>
    <div className="container">
      <div className={styles.centerHeader}>
        <Heading as="h2">A shared understanding—between humans and machines.</Heading>
        <div className={styles.lead}>
          RQML gives your project a single place to store the “why” and “what” of the system in a way that scales with
          your codebase and your team.
        </div>
      </div>

      <div className={styles.grid3}>
        {[
          {
            title: 'Clear structure',
            text: 'Goals, scenarios, requirements, interfaces, verification, traceability—organized so nothing important gets lost.',
          },
          {
            title: 'Low ceremony',
            text: 'Enough Requirements Engineering discipline to be useful—without turning spec writing into a second job.',
          },
          {
            title: 'LLM-ready',
            text: 'Predictable, structured context that improves codegen, refactors, tests, and architecture decisions.',
          },
          {
            title: 'Diff-friendly',
            text: 'Review requirements like code. PRs show exactly what changed and why.',
          },
          {
            title: 'Traceable',
            text: 'Connect goals → requirements → verification so you can prove what you built matches intent.',
          },
          {
            title: 'Toolable',
            text: 'Schema validation, editor support, CI checks, and automated consistency improvements.',
          },
        ].map((f) => (
          <div key={f.title} className={`card ${styles.featureCard}`}>
            <div className="card__body">
              <Heading as="h3" className={styles.cardTitle}>{f.title}</Heading>
              <p className={styles.cardText}>{f.text}</p>
            </div>
          </div>
        ))}
      </div>

      <div className={styles.ctaRow}>
        <a className="button button--primary" href="/docs/user-guide">
          Open the user guide
        </a>
        <a className="button button--secondary" href="/docs/reference">
          Browse the reference
        </a>
      </div>
    </div>
  </section>
  */}

  {/* HERO: HOW — moved up and retitled "Start in three steps" in the 2026-07 restructure.
      Original kept for reference:
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">Start right now</Heading>
        <ol className={styles.steps}>
          <li>
            <strong>Scaffold</strong> — <code>npx @rqml/cli init</code> drops a <code>requirements.rqml</code> and an <a href="/AGENTS.md" download><code>AGENTS.md</code></a> in your project root
          </li>
          <li>
            <strong>Check</strong> — <code>npx @rqml/cli check</code> validates the schema, coverage, and implementation drift (deterministic, offline)
          </li>
          <li>
            Develop with your <strong>coding agent of choice</strong> — it works from the spec, records trace links with <code>rqml link</code>, and you gate CI with <code>rqml check</code>
          </li>
        </ol>

        <div className={styles.punch}>
          <b>Tip:</b> Don't write RQML by hand. Your agent drafts it, <code>rqml skeleton</code> keeps
          the structure valid, and the <a href="#plugins">Claude Code and Codex plugins</a> run the
          loop for you — anchoring each session on the spec, validating every edit, and gating the
          turn on <code>rqml check</code>.
        </div>

        <div className={styles.heroCtas}>
          <a className="button button--primary button--lg" href="/docs/quick-start">Quick start</a>
        </div>
      </div>

      <div className={`card ${styles.snippetCard}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>A complete, valid spec</Heading>
          <CodeBlock language="xml">
{`<rqml xmlns="https://rqml.org/schema/2.2.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="https://rqml.org/schema/2.2.0
        https://rqml.org/schema/rqml-2.2.0.xsd"
      version="2.2.0" docId="DOC-HELLO-001" status="draft">
  <meta>
    <title>Hello World CLI</title>
    <system>hello</system>
  </meta>
  <requirements>
    <req id="REQ-HELLO-001" type="FR" title="Print greeting"
         status="draft" priority="must">
      <statement>
        The program MUST print "Hello, world!" to standard output
        and exit with status code 0.
      </statement>
    </req>
  </requirements>
</rqml>`}
          </CodeBlock>
        </div>
      </div>
    </div>
  </section>
  */}

  {/* SUPPORTING: USE CASES */}
  {/*
  <section className={`${styles.sectionAlt}`}>
    <div className="container">
      <div className={styles.centerHeader}>
        <Heading as="h2">Works especially well for</Heading>
        <p className={styles.lead}>Where intent gets lost, RQML keeps it explicit and reusable.</p>
      </div>

      <div className={styles.grid2}>
        {[
          {
            title: 'Greenfield projects',
            text: 'Reduce back-and-forth prompting, align faster, and establish a stable architecture narrative.',
          },
          {
            title: 'Refactors',
            text: 'Preserve intent while changing internals—so the “why” survives redesigns.',
          },
          {
            title: 'Teams & handoffs',
            text: 'Make decisions portable. New teammates and tools can catch up without archaeology.',
          },
          {
            title: 'Regulated / critical systems',
            text: 'Traceability + verification paths without heavyweight spec tooling overhead.',
          },
        ].map((u) => (
          <div key={u.title} className={`card ${styles.featureCard}`}>
            <div className="card__body">
              <Heading as="h3" className={styles.cardTitle}>{u.title}</Heading>
              <p className={styles.cardText}>{u.text}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  */}

  {/* SUPPORTING: MARKDOWN COMPARISON */}
  {/*
  <section className={`container ${styles.section}`}>
    <div className={styles.twoCol}>
      <div>
        <Heading as="h2">Why not just Markdown?</Heading>
        <div className={styles.lead}>
          Markdown is great for humans—but it’s flexible in ways that make it unreliable as machine-consumable system intent.
        </div>

        <ul className={styles.bullets}>
          <li>Markdown is <strong>ambiguous</strong> unless you enforce strict conventions.</li>
          <li>RQML is structured enough to be <strong>consumed consistently</strong> by tools and LLMs.</li>
          <li>You still get human context—just with a <strong>predictable shape</strong>.</li>
        </ul>

        <a className="button button--outline button--primary" href="/docs/reference">
          See the tag/attribute index →
        </a>
      </div>

      <div className={`card ${styles.card}`}>
        <div className="card__body">
          <Heading as="h3" className={styles.cardTitle}>The goal</Heading>
          <div className={styles.cardText}>
            Make the requirements spec the durable artifact. Let code be generated, regenerated, and refactored around it.
          </div>
          <div className={styles.quote}>
            “Stop re-explaining the same context to every tool, every time.”
          </div>
        </div>
      </div>
    </div>
  </section>
  */}

</div>
