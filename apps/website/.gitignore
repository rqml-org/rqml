# Dependencies
/node_modules

# Firebase (deploy cache only; firebase.json + .firebaserc are tracked so
# the GitHub Actions deploy can find them — see .github/workflows/firebase-*.yml)
.firebase/

# Github (for actions)
.github

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
