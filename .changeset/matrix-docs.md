---
"@rqml/schema": patch
---

Teach the bundled `AGENTS.md` template about `rqml matrix` — the traceability-matrix read surface added in 0.4.0 — so `rqml init` scaffolds a process contract that mentions it. The website tooling docs (`cli.md`, `mcp.md`) document the `rqml matrix` command and the `rqml_matrix` MCP tool alongside.
